box: debian
build:
  steps:
    - arjen/hugo-build:
        version: 0.14
        theme: casper
        flags: --buildDrafts=true
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        repo: onori/onori.github.io
        basedir: public
        domain: onori.github.io
