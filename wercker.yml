box: wercker/default
build:
  steps:
    - script:
        name: install git
        code: |
          apt-get update
          apt-get -y install git
    - arjen/hugo-build:
        version: 0.14
        theme: casper
        flags: --buildDrafts=false
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        repo: onori/onori.github.io
        basedir: public
