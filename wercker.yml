box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        theme: casper
        flags: --buildDrafts=false
deploy:
  steps:
    - uetchy/gh-pages:
        token: $GIT_TOKEN
        repo: onori/onori.github.io
        basedir: public
        domain: github.com
